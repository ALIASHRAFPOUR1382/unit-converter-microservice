# راهنمای مشاهده Response در Swagger UI

## مشکل: Response را نمی‌بینم!

اگر مراحل را انجام دادید اما چیزی نمی‌بینید، این راهنما را دنبال کنید:

---

## ✅ مراحل صحیح مشاهده Response

### 1. بعد از کلیک روی "Execute"

بعد از کلیک روی دکمه **"Execute"**، باید این بخش‌ها را ببینید:

#### بخش Response
- **Code**: کد HTTP (مثلاً 200، 201، 404)
- **Details**: جزئیات Response
- **Response body**: محتوای اصلی پاسخ

### 2. پیدا کردن Response Body

1. بعد از کلیک روی **"Execute"**، صفحه به پایین اسکرول می‌شود
2. بخش **"Responses"** را پیدا کنید
3. در این بخش باید ببینید:
   ```
   Code: 201
   Response body: {
     "id": 1,
     "title": "...",
     "description": "...",
     "completed": false,
     "created_at": "...",
     "updated_at": "..."
   }
   ```

### 3. اگر Response Body را نمی‌بینید

#### راه حل 1: اسکرول به پایین
- بعد از Execute، صفحه را به پایین اسکرول کنید
- Response body در پایین صفحه است

#### راه حل 2: بررسی Code
- اگر Code **201** یا **200** است = موفقیت
- اگر Code **404** است = Todo پیدا نشد
- اگر Code **422** است = خطای اعتبارسنجی (JSON را بررسی کنید)

#### راه حل 3: باز کردن Response Body
- روی فلش کنار **"Response body"** کلیک کنید
- یا روی **"Download"** کلیک کنید تا JSON را ببینید

---

## 📋 مثال کامل: ایجاد Todo

### مرحله 1: پیدا کردن Endpoint
```
POST /api/todos
```

### مرحله 2: کلیک روی "Try it out"
- دکمه آبی رنگ در سمت راست

### مرحله 3: ویرایش Request Body
```json
{
  "title": "تست",
  "description": "این یک تست است",
  "completed": false
}
```

### مرحله 4: کلیک روی "Execute"
- دکمه آبی رنگ در پایین

### مرحله 5: مشاهده Response
بعد از Execute، باید ببینید:

```
Responses
├─ Code: 201
├─ Details
│  └─ Response body
│     {
│       "id": 3,
│       "title": "تست",
│       "description": "این یک تست است",
│       "completed": false,
│       "created_at": "2025-12-11T22:10:00",
│       "updated_at": "2025-12-11T22:10:00"
│     }
```

---

## 🔍 تست سریع: بررسی اینکه API کار می‌کند

### روش 1: استفاده از مرورگر
مستقیماً این آدرس را در مرورگر باز کنید:
```
http://localhost:8000/api/todos
```

باید JSON لیست Todos را ببینید.

### روش 2: استفاده از PowerShell
```powershell
Invoke-RestMethod -Uri "http://localhost:8000/api/todos"
```

---

## 🐛 عیب‌یابی

### مشکل 1: Response body خالی است
**راه حل**: 
- مطمئن شوید که Code **200** یا **201** است
- اگر Code **404** است، Todo با آن ID وجود ندارد
- اگر Code **422** است، JSON شما اشتباه است

### مشکل 2: خطای CORS
**راه حل**: 
- سرور باید در حال اجرا باشد
- آدرس باید `http://localhost:8000` باشد

### مشکل 3: "Network Error"
**راه حل**:
- مطمئن شوید سرور در حال اجرا است
- در ترمینال باید پیام "Application startup complete" را ببینید

---

## 💡 نکات مهم

1. **همیشه Code را بررسی کنید**: Code 200/201 = موفقیت
2. **Response body را باز کنید**: روی فلش کلیک کنید
3. **JSON را کپی کنید**: می‌توانید Response را کپی کنید
4. **Schema را ببینید**: برای دیدن ساختار دقیق، روی "Schema" کلیک کنید

---

## 📸 چه چیزی باید ببینید؟

بعد از Execute موفق، باید این را ببینید:

```
✅ Code: 201
✅ Response body: (باز شده)
   {
     "id": 1,
     "title": "تست",
     "description": "این یک تست است",
     "completed": false,
     "created_at": "2025-12-11T22:10:00",
     "updated_at": "2025-12-11T22:10:00"
   }
```

اگر این را نمی‌بینید، صفحه را به پایین اسکرول کنید!


